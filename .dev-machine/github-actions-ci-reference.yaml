# =============================================================================
# ridecast2 -- GitHub Actions CI
# Generated by amplifier-bundle-dev-machine (optional, only when ci_enabled=true)
#
# Runs npm run build and npm run test on every push to main and on
# pull requests targeting main.
#
# Generation-time variables substituted here:
#   ridecast2    -- project identifier (used in workflow name)
#   npm run build   -- e.g. "pnpm build", "cargo build", "make"
#   npm run test    -- e.g. "pytest", "pnpm test", "cargo test"
#
# Copy this file to .github/workflows/ci.yaml in your project.
# =============================================================================

name: "ridecast2 CI"

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    name: Build and Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up environment
        # Add language-specific setup steps here.
        # Examples (uncomment and adapt):
        #
        # Python (uv):
        #   uses: astral-sh/setup-uv@v5
        #   with: { version: "0.10.4", enable-cache: true }
        #
        # Node.js (pnpm):
        #   uses: pnpm/action-setup@v4
        #   with: { version: 9 }
        #
        # Rust:
        #   uses: dtolnay/rust-toolchain@stable
        run: echo "Add setup steps for ridecast2 above"

      - name: Install dependencies
        # Replace with the lock-file install command for your project.
        # Examples:
        #   uv sync --extra dev
        #   pnpm install --frozen-lockfile
        #   cargo fetch
        run: echo "Replace with your install command (see Dockerfile for the correct command)"

      - name: Build
        run: "npm run build"

      - name: Test
        run: "npm run test"
